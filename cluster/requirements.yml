---
# Ansible Galaxy collections used by the roles.
#   ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix       # mount module
  - name: community.general   # filesystem, parted
